# Exact reviewed dependencies from RULES.md. No git/path/features overrides.
# The trusted gate checks the allowlist, licenses and resolved dependency tree.
[dependencies]
feral-amd = "0.2.1"
feral-amf = "0.2.1"
feral-metis = "0.2.1"
feral-kahip = "0.2.1"
feral-ordering-core = "0.2.1"
feral = "0.11.0"
