# 0133 — iter108 terminal final subtree refine (Xo1otl family)

## Context
iter106 micro-bip only — do not submit. Consider-tax reclaim dying.
Xo1otl beat our `0fb513b` with this single family: refine shipped incumbent at end of `leader_order`.

## Leap
`FINAL_REFINE_MAX_WORK = 400_000`; after late polish, postorder + two independent `subtree_refine` cfgs (chain cfg + terminal deep). Strict exact admit. Base = promoted `4ecc26e`/`0fb513b` only.

## Target
~2.8 bip vs promoted twin; ≥15 movers; worst ≤1.10 prefer ≤1.00; beat lead 0.849495.

## Result
(pending)
