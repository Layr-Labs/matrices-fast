# 0146 — Hold 10 % 1b on mid x-set KKTs so lee4_06 takes 4b

- **Date:** 2026-09-09
- **Score:** 273a local 0.794136 → v19 **0.794085** (−0.51 bip vs this-box 273a);
  v17 global 40 % was 0.794152 (loss). Hidden pending on v21 (hold only; AmindNorm dropped after 302a fail).
- **Status:** win (dev); hidden pending
- **Files:** `mod.rs` (`INDEP_IMMEDIATE_HOLD`, `mid_xset_hold`),
  `indep_first.rs` (x-caps 15/5 only n<=10k; v20 AmindNorm on 16k<n<=18k nnz<=80k)

## Hypothesis

273a's 10 % 1b-accept is right for lee2_06 (13 % lead, wants subtree on the
lift) and gams05 (32 %, same) and wrong for lee4_06 (21 %): subtree on that
lift finishes 0.5074, 4b finishes 0.5044. A global 40 % floor (v17) flips
all three and loses the local score. Size-band the 40 % floor to the mid
x-set slice `n in [10000, 12000) && nnz <= 80_000`.

## What changed

Default immediate stays `(9,10)`. On the hold band, require `(3,5)` (40 %)
so lee4_06 defers to 4b. Extra excluding caps 15/5 move to n<=10k so they
do not re-roll the 4b lift. METIS/metric envelope stays 273a (no skip-METIS).

## Result

| row | 273a 10 % | v17 40 % | v19 hold |
|---|---:|---:|---:|
| crudeoil_lee4_06 | 0.5074 | 0.5044 | 0.5044 |
| crudeoil_lee2_06 | 0.7586 | 0.7668 | 0.7586 |
| gams05 | 0.5276 | 0.5299 | 0.5276 |
| SCORE | 0.794136 | 0.794152 | **0.794085** |
| WORST | 1.014–1.105 s | 1.015 s | 1.081 s |

## Why it won

Subtree-on-lift vs later-stage polish is not monotone in the 1b lead. lee4_06's
lift is real (subtree on the old incumbent gains < 1.5 %) but the lift's own
subtree starting point converts less under transplant/minl. Holding 1b is
cheaper than re-running subtree on the lift after 4b.

## Follow-ups

- Tight AmindNorm on 16k<n<=18k nnz<=80k (edgecross24-115) stacked as v20.
- Do not skip METIS on the lee4_09/10 band (hidden timeout family).
- 4b leftover subtree gated to n<=8k still untested; lee2_06 already 1b.
