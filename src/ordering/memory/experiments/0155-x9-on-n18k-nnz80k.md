# 0155 — x9 only on n>18k and nnz<=80k, paid by SqDiv+DegDivNvDegme

- **Date:** 2026-09-09
- **Base:** tip `be9bae1` official local **0.794121** / fill **0.926126**. Hidden best 0.843577. Promote bar 0.843476.
- **Score:** official local **0.794121** / fill **0.9261** (printed; buckets identical). Exact tie. 0 movers vs tip.
- **Status:** null. Reverted to tip `be9bae1` ordering sources. Not submitted.

## Hypothesis

Second-colour is admitted only for `n <= 18_000 && nnz <= 80_000`. The uncovered
class is `n > 18_000 && nnz <= 80_000` (dev: gasprod_sarawak81, nd_netgen-2000-3-4-b-a-ns_7,
emfl100_5_5, crudeoil_pooling_dt2). lee4_09/10 stay out (nnz>80k). Not an x9
add on lee4_09. Not the superseded DegP075→DegP125 swap.

On that class only, admit x9 (`greedy_independent_set_excluding` cap 9). Those
new x9 cores get only the census trio DegDivNvSqrtWf, DegPlusDegme, DegSqrt,
and only inside `METRIC_CORE_MAX_N` / `METRIC_CORE_MAX_NNZ`. Dense/hub cores
(max degree >= 1000, popdynm200-class) and the 0.7s cnnz band (`cnnz >= 200_000`,
the 0145 0.6 s/pass cliff) stay AMD-only. Pay by dropping SqDiv and
DegDivNvDegme on every n>16k metric core. n<=16k list unchanged. Caps 15/5
untouched. No matrix-id gate.

Relabelled-AMF `dense_alpha` is already multi-α on the tip (`[5.0, 2.0, -1.0, 1.0, 16.0]`
plus the AMF sweep). No extra alpha pass.

## What changed

`src/ordering/indep_first.rs` only. Structural n/nnz gate. Deterministic.
Yukon run logged to `/tmp/yukon-run-0155.log`.

## Result

Official local **0.794121** / fill tiebreak **0.9261**. Exact bit-identical to
tip on all 300 rows (0 movers). Class rows unchanged: gasprod_sarawak81 0.914,
nd_netgen-2000-3-4-b-a-ns_7 0.949, emfl100_5_5 1.000, crudeoil_pooling_dt2 0.664.
lee4_09 0.645, lee4_10 0.639, popdynm200 0.953 unchanged. Buckets unchanged
(lt_1k 0.8875 / 1k_10k 0.8398 / gt_10k 0.6898). Worst `order()` not printed
(table column is `(capped)`); timing is not claimed. Keep rule needs more than
one class row to move. 0 < 2, so reverted. Not submitted.

## Why it won / lost

Lost / null. The new x9 set either size-deduped, failed admission, or its
AMD / census-trio walk never beat the incumbent. Dropping SqDiv and
DegDivNvDegme on other n>16k metric cores was score-flat. DegP075 was not
replaced. Reverted. Tip-clean `indep_first.rs`.
