# 0173 — 0171 + FINAL_FIVE OPS192M + SUBTREE150k (no AMF)

- **Date:** 2026-09-09
- **Base tip:** `74b6ccd` (board best 0.84349 / 723bf797/475a; local tip 0.793834)
- **Score:** 0.793834 → **0.793755** (−0.79 bip)
- **Status:** miss / reverted. Not submitted.

## Hypothesis

0171 lean 488 package was clean **8/0** at local **0.793785** (−0.49 bip) but thin vs ≤0.793734 / ≥0.8 bip. 0172 stacked 0163 SUBTREE150k + additive AMF α tickets and lost 0-worse (12/2, −0.26 bip); AMF is banned for this stack. Reuse 0171 A–D, raise FINAL_FIVE_OPS 128M→**192M** (jonathan CI ceiling, still ≤192M ban line), and add 0163-only SUBTREE150k first-round replacement of late PEO-large. No AMF α tickets.

Target: local ≤ **0.793734** or ≥0.8 bip with 0 worse; abort submit if worst order() > **1.105s** (probe when score ≥0.5 bip / bar gate).

## What changed

`src/ordering/mod.rs` only:

**Restore 0171 A–D:**
1. **mid_force:** `(7_000..22_000).contains(&n) && nnz >= 48_000`
2. **FINAL_FIVE:** `MAX_N=14_000`, `MAX_NNZ=100_000`, **OPS=192_000_000** (raised from 0171's 128M)
3. **REDUCE deep:** `REDUCE_RECURSE_MAX_NNZ` 150k→**250k**; `REDUCE_RECURSE_DEEP_MAX_CORE_N` 80k→**120k**; `DEEP_MAX_CORE_NNZ` stays 250k
4. **Conditioned terminal re-transplant (0154c):** after phase-14 `refine_with_donors`, snapshot `transplant_entry_flops` and clone `donor_perms` when non-empty; before final return, re-call only if `best_flops < transplant_entry_flops`; admit on bijection + strict `<`. No `TRANSPLANT_LEDGER` widen; not unconditional.

**NEW E — subtree-chain past 45k (0163 semantics only):**
- `SUBTREE_CHAIN_MAX_N` 45_000→**150_000**
- `SUBTREE_CHAIN_ROUNDS_MAX_N` = **45_000** (follow-up rounds + improved==0 retry + MINL subtree ticket)
- Historical full chain: `n ≤ 45k && nnz ≤ 1.5M`
- Extended first-round only: `45k < n ≤ 150k && nnz < 1.2M`; late PEO-large skipped on that slice
- No AMF α tickets

Unchanged / closed: PAIR ops, extrarelbl, gasband16k, heavy-metric invention, unconditional re-transplant, AMF additive tickets, jonathan mpbp_15 fingerprint.

## Result

Yukon local **0.793755** vs tip rebaseline **0.793834** (`/tmp/yukon-run-0173.log`, tip `/tmp/yukon-run-tip-74b6ccd.log`). Fill 0.9258. Buckets: lt_1k 0.8875, 1k_10k 0.8398, gt_10k 0.6890.

Flops-exact vs tip: **20 better / 0 worse / 280 same**.

Better (tip→0173): crudeoil_lee4_06 (−154155), nuclear10a (−104139), cont6-qq (−19824), transswitch2736spr (−4277), crudeoil_lee2_06 (−2568), mpbp_15 (−2328), transswitch2383wpr (−1983), chimera_selby-c16-02 (−722), mpbp_34 (−537), chp_shorttermplan2a (−345), mpbp_35 (−290), glider400 (−263), transswitch0300p (−257), chimera_selby-c16-01 (−239), powerflow0300p (−221), rsyn0840m04m (−110), crudeoil_pooling_ct3 (−93), chp_partload (−88), sporttournament18 (−30), chimera_k64ising-02 (−23).

n>45k vs tip: **3 better / 0 worse** (cont6-qq, transswitch2736spr, transswitch2383wpr) — matches 0163 isolate.

vs 0171 log: **13 better / 0 worse / 287 same**. Retains 0171's clean 8/0 set; E adds the three n>45k wins; OPS192M + widen adds mid-n movers (lee2_06, mpbp_*, chp_*, powerflow/transswitch0300p, rsyn0840m04m, chp_partload) with 0 worse.

Short of ≤0.793734 (1 bip; need −0.000100, got −0.000079) and ≥0.8 bip strong-submit (0.79 bip). Timing not probed (score bar missed). `src/ordering/mod.rs` restored to tip `74b6ccd`.

## Why it won / lost

Cleanest stack this tip: **20/0** and −0.79 bip beats 0171 (−0.49, 8/0) and 0172 (−0.26, 12/2). Dropping AMF restored 0-worse; OPS192M on the widened FINAL_FIVE gate paid mid-n wins without regressions; 0163 E again moved exactly the three public n>45k rows. Absolute geomean still 0.21 bip short of ≤0.793734 and 0.01 bip short of the ≥0.8 bip strong gate — not enough for submit.

## Follow-ups

- Do not submit. Keep page; tip restored.
- No 0174: 0 worse, no lone regression to gate out; bar still out of reach by construction (need ~0.21 bip more).
- Do not re-add AMF α tickets on this stack (0172 regressions).
- OPS>192M still banned.

## Links

- [0171](0171-lean-488-conditioned-retransplant.md) base stack (0.793785 / 8/0)
- [0172](0172-0171-base-subtree150k-amf-alpha-tickets.md) 0171+E+AMF (worse; AMF banned)
- [0163](0163-subtree-chain-past-45k.md) subtree-chain replacement semantics
