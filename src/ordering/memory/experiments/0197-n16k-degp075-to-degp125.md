# 0197 — n>16k metric last slot DegP075 → DegP125

- **Date:** 2026-09-09
- **Base tip:** `74b6ccd` (local tip 0.793834). linson007 `38a3c21` failed (no promote).
- **Score:** 0.793834 → **0.793834** (0.00)
- **Status:** miss / last-slot branch reverted. Not submitted. Model Grok 4.6, harness Grok Bot.

## Hypothesis

On competitive indep_first metric cores the six-variant list ends in `DegP075`.
`metric_k` stays tip (`4` on `n≤16k`, `3` above). Same-count swap the last
slot to `DegP125` (`deg^1.25`) on **all** `n>16k` metric cores only — not the
narrow 16k–20k DegDivNvWfP15 miss [0179], not a 4th core / SqDiv-drop [0196],
not HEAVY_METRIC_ORDER. Slot fires whenever a competitive metric core is
scheduled on n>16k, so more frequent than last-ticket HEAVY redraws without
growing task count.

## What changed

`src/ordering/indep_first.rs` only (then restored). `HEAVY_METRIC_ORDER` /
`mod.rs` / `probe.rs` untouched. `REDUCE_EXTRA_DEPTHS` stays `[5,4,2,6]`.
Exact-search seeds alone.

During the run, when building the metric task list:

- `n≤16k`: tip six including `DegP075`
- `n>16k`: same six with last slot `DegP075` → `DegP125`

`metric_k` unchanged. Other five variants unchanged. No DegDivNvWfP15, no
4th core, no SqDiv drop. After miss, tip list restored (DegP075 everywhere).

## Result

Yukon local **0.793834** vs tip **0.793834** (`/tmp/yukon-run-0197.log`).
Buckets unchanged: lt_1k 0.8875 / 1k_10k 0.8398 / gt_10k 0.6891. Fill 0.9258.

Flops-exact vs tip `/tmp/yukon-run-tip-74b6ccd.log`: **0 better / 1 worse / 299 same**.
n>16k: 0 better / 1 worse (`gabriel09` 26822016→26822516). Keep bar missed
(need beat 0.793834, >1 n>16k mover, 0 worse).

## Why it won / lost

Same basin as [0168](0168-0148-sqdiv-degp125-gasband16k.md): DegP125 on n>16k
touches gabriel09 and does not help. Unlike 0168 (SqDiv↔DegP125), this kept
SqDiv and only swapped the DegP075 last slot — still one regression, zero
wins. DegP075 is not a free redraw on this tip for n>16k.

## Follow-ups

- Do not retry DegP125 for DegP075 on all n>16k without a fresh census that
  excludes gabriel09-style losses.
- dens≤4 / mid-K4 / Ammf / wf01 / cm_sqdiv / depth-7 / 4th-metric-core+SqDiv-drop remain closed.
- Tip ordering logic restored (DegP075 last slot everywhere).

## Links

- Prior DegP075 band swap miss: [0179](0179-ndivwfp15-on-n-gt-16k.md)
- Prior SqDiv↔DegP125 miss: [0168](0168-0148-sqdiv-degp125-gasband16k.md)
- Prior 4th-metric-core revert: [0196](0196-fourth-metric-core-n-gt-16k.md)

## Submission

Not submitted. Last-slot branch reverted only.
