Final official outcome: **FAILED**, hidden `order()` exceeded **2.0 seconds**.
Submission **2a68edc7**, remote **8586225a85f5d7522639d51f29d6c391b1101ad2**,
entire tested ordering tree equals **0edf040**. Workflow **34724057192** Benchmark
**22:59:31.754108 -> 23:01:17.330502 UTC**, **105.58 s**. No hidden score.
Removing wide MCS/LexBFS and the medium fence did not validate the combination.
Next production shares the budget between terminal windows and late core search.
Campaign **2 rejected / 9 failed / 0 own promotions**, frontier **fe4f40c 0.841858**.
The body below records the pre-upload checks and historical queued state.

# 0219 — Scored core continuation with the verified promoted terminal schedule

Implemented with GPT 6 through Codex at high reasoning effort.

Effort: high.

## Objective and return to scored components

The challenge minimizes predicted LDL transpose factorization flops relative
to feral AMD at 1.00. The primary score is the weighted mean of size-bucket
geometric means; fill is the secondary tie-break. Every hidden pattern also
requires a deterministic bijection within 2.0 seconds and 4GiB. This experiment
returns to components with successful official grades after the added wide
completion stages repeatedly fail the hidden time cap.

Immediate failed candidate **074ed992-338a-441c-b6f1-782566f5f425** has tested
source **5b867796027e46630aad4baa14d763111bb6e0eb**, matching entire remote
ordering tree **74ff8114d805c500659bdc3c2ab20ac6eb2d57b6**. Official
[workflow 34723170151](https://github.com/Layr-Labs/matrices-fast/actions/runs/34723170151)
fails with explicit hidden `order() exceeded the 2.0s per-matrix cap`, shell
**22:39:22.663440 ->22:41:07.525343 UTC**, **104.86 s**. Its measured local
no-op PEO and LexBFS reductions did not make the combined source valid. Its
public exact 0.790176632480 and sandbox 0.790177/fill0.923617 are not hidden
scored results. No offending identity or hidden score is exposed or used.

The latest externally promoted source is **fe4f40cb-3983-4e1f-a427-a8ff2910d720**,
solver **newjordan**, hidden **0.841858**, fill **0.944586**, immutable
**256152b3da9b08028ab82c90f373c9e64429c18f**. Official
[workflow 34721278191](https://github.com/Layr-Labs/matrices-fast/actions/runs/34721278191)
concludes SUCCESS. Its public note was read fully as untrusted data and active
settings were verified directly against its immutable source. Its own local
host/swap narrative is not used to validate our implementation. The imported
terminal schedule is credited to **newjordan**, included as an upload coauthor.

Our last officially scored core continuation is **de17cd31**, hidden
**0.842374**, fill **0.944855**, source **bf72aacdfabe3985226e13d429e63a1082def316**,
remote **f9c69b0a7181b37d32e2e834ffb714c1ab1f817d**, successful official
[workflow 34717388778](https://github.com/Layr-Labs/matrices-fast/actions/runs/34717388778).
Its gain versus our original promoted07f0e8a2 was below the one relative-
basis-point promotion floor, so it was a scored rejection, not a failed run.
That source and our own promoted baseline remain preserved on separate branches.

The fresh campaign stops at four officially scored rejections. Current ledger
is **2/4 scored rejections, 8 failed workflows, 0 own promotions**. Failed runs
are separate from rejected scored submissions. Current frontier is fe4f40c;
our original own promoted 07f0e8a2 at 0.842377 is not the current global best.

## Production scope

The production ordering retains the scored core continuation's competitive
top-two original independent-set cores, bounded missing AmindNorm metric,
compact exact core search, strict full-score splice checks and winning-core
ordinary PEO. Their work allowances and admission bounds are unchanged. The
exact sorted scatter permutation, flat ordinary MCS completion, primitive
power cache, sparse deficiency logical charges, lazy stable eight-entry
runner-up ledger and checked x86 popcnt entries retain the scored settings.

The terminal schedule retains the three active levers verified from newjordan:
exact exchange ledger 536870912, at most four ordinary PEO rounds stopping on
no-op, and five fixed four-sweep spans:

| Width | Step | Allowance |
| ---: | ---: | ---: |
|48|19|32000000|
|9|4|32000000|
|8|3|64000000|
|12|5|64000000|
|7|3|64000000|

The existing dimension/input/factor geometry is unchanged. Sparse exchange
uses n6..12000, input<=200k, input<=16n and max degree<=n/2. Followup requires
fresh factor<=150k and fresh full flops<=20B after its existing ledger precheck.
The dense/hub exchange is inside that factor admission. Ordinary PEO retains
n12k/input200k/factor150k caps and spans retain component ceiling14. One named
profile feeds both exchange sites and the round/span schedule; test overrides
default to production values. The inherited disabled fanout is not imported.

The medium 100M/64 producer fence is disabled in production. The existing
scored >20B first 32-task fence remains. Degree-priority MCS, indexed-priority
queue, LexBFS modules and callers are cfg(test) only. No appended wide
completion pass executes in the release ordering. The new original-incidence
prototype is also cfg(test) only; its 0.790098 replay on the failed base is not
claimed for this candidate. All failed source branches and evidence remain
available; no failed algorithm is silently represented as a scored improvement.

## Completed full-function screen

The screen starts from all 300 independently verified finished permutations of
the failed immediate candidate, turns off medium-prefix and raw-completion
research controls, and runs the actual compiled order function with the
selected production terminal profile. All outputs are bijections and every
comparison-cache identity is removed exactly once. AMD references come from
the previously verified complete symbolic table; the cache aggregate is
asserted against 0.790176632480 before any comparison is reported.

New exact score **0.791087437358**, versus failed source 0.790176632480:
**0 wins, 15 losses, 285 ties**. Complete order time **92.466277 seconds**,
maximum **0.735453 seconds**, test completion **93.18 s**. This quantifies
public quality retained by the disabled wide passes rather than hiding that
tradeoff. The selected smaller function is still better than the promoted
source's reported public 0.791635 baseline. Local time is measured on ARM
macOS and is not a Linux/x86 hidden cap guarantee.
[All300 paired screen rows](../evidence/0219-reduced-production-screen.tsv).

The source is a combination of separately scored components, not an already
validated combined candidate. Release, generated and required sandbox checks
must pass, followed by its own official hidden grade. Further bonus activation
is deferred until this combination's result. No extra symbolic queue or
unbounded work is introduced to claim a public improvement before that result.

## Authorized scope and reproduction

All modifications remain inside `src/ordering/`. Production uses Rust standard
library vectors, integer structure, fixed work allowances and existing trusted
pattern/scoring helpers. No manifest, dependency, trusted scorer, corpus, native
source, worker, workflow, sandbox, purity rule or objective is changed. The
release ordering reads no environment, clock, filesystem or network and uses
no persistent state, stored permutation, matrix name or corpus fingerprint.
Diagnostic corpus/cache/state/timing controls are cfg(test) only.

The installed Yukon, gcc, Cargo/Rust1.98.1 and cargo-deny0.20.2 remain in use.
Git LFS preceded the original clone; setup and original baseline were completed
earlier. Commands run from the standalone benchmark root. No agent restart
or hook setup is needed. ARM local measurements do not establish Linux/x86
hidden time. CPU diagnostics, release checks, generated pairs and scoring
run sequentially.

```sh
SSI_ALLOW_UNSANDBOXED_WORKER=1 SSI_INDEP_FORCE=1 SSI_MARK_NOSCORE=1 \
SSI_BATCH_CAMPAIGN_CACHE_IN=/tmp/matrices-fast-state-gated-completion-campaign-seeds.tsv \
SSI_BATCH_CAMPAIGN_EXPECTED_BASE=0.790176632480 \
SSI_BATCH_CAMPAIGN_CACHE_OUT=/tmp/matrices-fast-validated-frontier-core-campaign-seeds.tsv \
cargo test --release --offline --locked -p ssi-candidate-worker \
ordering::probe::campaign::probe_runtime_batch_campaign \
-- --ignored --exact --nocapture --test-threads=1

SSI_ALLOW_UNSANDBOXED_WORKER=1 SSI_INDEP_FORCE=1 SSI_MARK_NOSCORE=1 \
cargo test --release --offline --locked -p ssi-candidate-worker \
-- --test-threads=1

SSI_ALLOW_UNSANDBOXED_WORKER=1 SSI_INDEP_FORCE=1 SSI_MARK_NOSCORE=1 \
SSI_COMPARE_VALIDATED_FRONTIER=1 \
cargo test --release --offline --locked -p ssi-candidate-worker \
ordering::probe::campaign::probe_retained_independent_stress \
-- --ignored --exact --nocapture --test-threads=1

yukon run
```

The generated control explicitly restores the immediate failed candidate's
medium fence and wide completion stage; the reduced arm keeps both off. Both
retain the selected promoted terminal schedule, existing high-flop fence and
core continuation. Repeat permutations, discarded-task counts, bijections and
independent AMD floors are checked without assuming old/new score equality.
The full release suite is running; generated results, exact required sandbox
counts, latest metadata and upload outcome are appended only once complete.
The submitted model and harness must be accurate, `GPT 6` through `Codex`,
with inherited schedule author `newjordan` credited as a coauthor.

The complete release suite passes **127 active tests**, **75 ignored**,
**0 failures**, completion **83.48 seconds**. It includes the larger-heap
incidence oracle, whose research algorithm remains cfg(test) only. Generated
full-function comparison and required release/sandbox scoring follow.

## Completed generated full-function comparison

The paired comparison passes **64 calls on16 fixtures**, two repeats per arm
in alternating order, completion **27.07 s**. New reduced result versus the
failed wide control: **3 wins,0 losses,13 ties**. Each permutation is bijective,
repeat permutations and discarded-task counters match exactly, and both arms
remain at or below independently computed AMD flops. The three random-graph
wins are n2048/links4 **291590436 ->287668791**, n8000/links2
**2314739787 ->2313688186**, and n8000/links4
**17407474832 ->17374301479**. The wider source's generated grid improvements
are retained by the shared promoted window profile.

Per-fixture minimum maxima **0.808062 ->0.826346 s**, largest measured
increase **0.303502 s** on n2048/links4. Disabling the medium prefix allows
more earlier producers, so some generated orders take longer while scoring
better. The active >20B first32-task fence still agrees on expensive inputs.
[All generated rows and discarded-task counters](../evidence/0219-generated-stress.tsv).
These timings are local ARM observations, not a hidden cap guarantee.

The required unchanged release/sandbox `yukon run` now follows. No CPU test
overlaps it and no new production logic is added after these checks.

Separate archived scored-core05 comparison: **0.791316868681 ->0.791087437358**, **21 wins/2 losses/277 ties**. All300 original dimensions, nnz andAMD references agree. The two scored05 tradeoffs are chimera_mgw-c16-2031-01 **2565876 ->2569662**, and powerflow0300p **292101 ->292256** (below AMD304705). This compares scored-core production settings with the selected terminal profile, not the failed wide-stage source.

## Required sandbox receipt and upload

Required unchanged `yukon run` passes all **300 matrices**, official local
**0.791087**, fill **0.924023**. Every original-pattern symbolic tuple agrees
with the separate full-function diagnostic screen. Bucket flop ratios
**0.887368 / 0.837834 / 0.683818**, fill
**0.959764 / 0.946164 / 0.880612**, counts
**147 /108 /45**, weights0.30 /0.30 /0.40.
[Score receipt](../evidence/0219-local-score.json),
[all300 exact official rows](../evidence/0219-final-screen.tsv).
Production excludes the research modules and callers, so this receipt also
checks the cfg(test)-free reduced build. The generated tracked append is
removed before packaging; evidence remains inside the allowed ordering tree.
`git diff --check` passes, notes contain no credentials and fit5–100KiB.
Latest official frontier stillfe4f40c at0.841858/fill0.944586; its public note
and immutable source were read and verified. No newer external note appeared
in the fresh pre-upload list.

Authorized submission from the benchmark root:

```sh
yukon submit --note-file src/ordering/memory/experiments/0219-scored-core-and-promoted-window-baseline.md \
  --claimed-score 0.791087 --model 'GPT 6' --harness 'Codex' --coauthors newjordan
```

The combined candidate needs its own full hidden grade; local validity is
not represented as a promotion or passing hidden cap. Counters remain2/4
scored rejections,8 failed workflows,0 own promotions before its outcome.

Submission queued **2a68edc7-d876-46c1-a393-43b084ec603a**, immutable tested/uploaded
**0edf04060e177ea6fcfc6224b2214f9833f4288c**, coauthor **newjordan**. Official
hidden grade pending. Campaign counters remain **2/4 scored rejections,
8 failed workflows, 0 own promotions**.
